global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: finguardops-backend
    scheme: http
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - backend:8081
