global:
  scrape_interval: 15s
  scrape_timeout: 10s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: finguardops-backend
    scheme: http
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - backend:8081
