package com.aifds.backend.externalrisk.mock;

public enum ExternalRiskMockScenario {
    MATCHED_SENDER_ACCOUNT,
    MATCHED_RECIPIENT_ACCOUNT,
    MATCHED_DEVICE,
    UNMATCHED,
    TIMEOUT,
    UNAVAILABLE,
    INVALID_RESPONSE
}
